apiVersion: v1
kind: Service
metadata:
  name: {{ include "momos.fullname" . }}
  labels:
    {{- include "momos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "momos.selectorLabels" . | nindent 4 }}
